apiVersion: apps/v1
kind: Deployment
metadata:
  name: flowfi-indexer
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flowfi-indexer
  template:
    metadata:
      labels:
        app: flowfi-indexer
    spec:
      containers:
        - name: indexer
          image: flowfi/indexer:latest
          envFrom:
            - configMapRef:
                name: flowfi-env
            - secretRef:
                name: flowfi-secrets
